

1. **字典分类功能测试**
   1.1 字典分类创建功能
   - 验证能成功创建字典分类
   - 验证分类名称在应用下的唯一性校验（重复时提示错误）
   - 验证必填字段校验（分类名称、所属应用）

   1.2 字典分类编辑功能
   - 验证能成功编辑字典分类
   - 验证编辑时分类名称在应用下的唯一性校验
   - 验证必填字段校验

   1.3 字典分类删除功能
   - 验证无字典的分类可正常删除
   - 验证有字典的分类删除时提示错误（需先清空字典）

   1.4 字典分类查询功能
   - 验证按分类名称/描述模糊搜索
   - 验证重置按钮清空搜索条件
   - 验证树节点点击联动右侧列表

   1.5 排序规则验证
   - 验证应用按排序字段/创建时间正序
   - 验证分类列表默认按创建时间倒序

2. **字典管理功能测试**
   2.1 字典创建功能
   - 验证必填字段校验（字典分类、编码、名称）
   - 验证字典编码在应用下的唯一性
   - 验证显示模式默认值（穷举）

   2.2 字典编辑功能
   - 验证字典编码唯一性校验（编辑时）
   - 验证状态切换功能（启用/禁用）

   2.3 字典删除功能
   - 验证无字典值的字典可删除
   - 验证有字典值的字典删除时提示错误

   2.4 字典查询功能
   - 验证按编码/名称模糊搜索
   - 验证按状态筛选

3. **字典值管理功能测试**
   3.1 字典值创建功能
   - 验证必填字段校验（名称、编码）
   - 验证编码在应用下的唯一性
   - 验证所属上级自动继承规则（根节点/父节点）

   3.2 字典值删除功能
   - 验证未被引用的字典值可删除
   - 验证被引用的字典值删除提示

   3.3 层级结构测试
   - 验证字典值树形结构展示
   - 验证子项新增时自动关联父级

4. **通用交互测试**
   4.1 弹窗功能
   - 验证所有弹窗的取消按钮功能
   - 验证输入建议文本显示

   4.2 状态一致性
   - 验证禁用状态的字典/字典值是否生效

   4.3 组织适用性
   - 验证未选择适用组织时默认全局适用

5. **错误场景测试**
   5.1 重复名称/编码
   - 验证所有唯一性校验场景的报错提示

   5.2 非法操作拦截
   - 验证有子项时删除父项的拦截逻辑

6. **数据展示测试**
   6.1 树形结构展示
   - 验证应用-分类两级树节点展示
   - 验证字典值默认折叠/展开规则

   6.2 列表排序
   - 验证所有列表默认排序规则（创建时间倒序）
   - 验证字典值按显示顺序排序

注：原型图示相关测试需求（如UI布局、元素样式等）未包含在本列表中，需结合具体UI设计文档补充。